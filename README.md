# kbot

installation on mac

```
git clone git@github.com
pip3 install 'bs4';cd ~;mkdir bin;echo "export PATH=PATH:~/bin" > .profile`
wget -O ~/bin https://chromedriver.storage.googleapis.com/2.30/chromedriver_mac64.zip;unzip chromedriver_mac64.zip;
```
